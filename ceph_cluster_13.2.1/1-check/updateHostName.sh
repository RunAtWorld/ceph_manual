sudo cat > /etc/hostname<<EOM
micros-k8s-1
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-2
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-3
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-4
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-5
EOM

sudo cat > /etc/hostname<<EOM
micros-k8s-6
EOM
